{
  "name": "update_user_account_by_sys_id",
  "description": "Finds one or more user records from the sys_user table using any identifying field. Supports sys_id, email, user_name, name, first_name, last_name, or custom fields.",
  "strict": false,
  "parameters": {
    "type": "object",
    "properties": {
      "filters": {
        "type": "object",
        "description": "Key-value filters to find the user(s). Can include standard fields like 'email', 'user_name', 'name', or any custom field.",
        "minProperties": 1,
        "additionalProperties": true
      }
    },
    "required": [
      "filters"
    ]
  }
}
